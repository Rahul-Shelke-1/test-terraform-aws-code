## Fixtures

Fixtures provide different input configurations for testing.

For example:

```
fixtures/
├── basic.tfvars
├── encrypted.tfvars
├── versioning.tfvars
├── lifecycle.tfvars
└── replication.tfvars
```

Your tests can reuse these inputs instead of hardcoding values.
